[project]
name = "dbgr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.0.0",
    "textual-dev>=1.7.0",
    "textual>=3.1.1",
    "pandas>=2.2.3",
]

[dependency-groups]
dev = [
    "ruff>=0.11.6",
]
